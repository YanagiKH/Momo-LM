[package]
name = "momo-core-rust"
version = "0.2.0"
edition = "2021"
description = "Memory-safe tensor kernels for Momo-LM"
license = "MIT"
publish = false

[lib]
name = "momo_core_rust"
crate-type = ["cdylib", "rlib"]

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = true
